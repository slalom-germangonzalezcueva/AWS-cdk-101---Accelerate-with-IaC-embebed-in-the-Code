{
  "name": "cv-updater-service",
  "version": "1.0.0",
  "private": true,
  "description": "Application/business logic for the CV Updater demo. CDK packages these handlers into Lambda functions.",
  "scripts": {
    "build": "tsc --noEmit",
    "check": "tsc --noEmit",
    "lint:deps": "npm audit --audit-level=high"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.782.0",
    "@aws-sdk/lib-dynamodb": "^3.782.0",
    "@aws-sdk/client-s3": "^3.782.0",
    "@aws-sdk/s3-request-presigner": "^3.782.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.147",
    "@types/node": "^22.14.0",
    "typescript": "^5.8.3"
  }
}